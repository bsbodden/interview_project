<h1>Get your population numbers here</h1>
<%= form_tag(populations_path, method: "get") do %>
  <%= text_field_tag(:year) %>
  <%= button_tag("Submit", type: 'submit', name: 'submit') %>
<% end %>

<% if @year && @population %>
<h3>You requested the population for: <%= year %></h3>

<h3>Population: <%= population %></h3>
<% end %>
